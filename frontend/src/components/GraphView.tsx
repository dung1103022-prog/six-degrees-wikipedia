// Phase 4 scaffold: Sigma.js + Graphology view of a SearchResponse (SPEC ADR-014). Not implemented.
// Tests replace this module (Sigma needs WebGL, which the DOM test environment lacks).
import type { SearchResponse } from "../api/types";

export default function GraphView(_props: { response: SearchResponse }): null {
  return null;
}
